\documentclass{article}
\usepackage{pgfplotstable}
\usepackage{tipa}
\usepackage{colortbl}
\usepackage{booktabs}

\begin{document}
\section{Lexicon}
\pgfplotstabletypeset[
	col sep=tab,
	string type,
	sort,
	sort key=ipa,
	columns={ipa,pos,trans,eg},
	sort cmp=string <,
	every even row/.style={
		before row={\rowcolor[gray]{0.9}}
		},
	columns/ipa/.style={
		column name=IPA LaTeX, column type={l},
		postproc cell content/.style={@cell content=\textipa{##1}}
		},
	columns/pos/.style={column name=Part of Speech, column type={l}},
	columns/trans/.style={column name=English Translation, column type={l}},
	columns/eg/.style={column name=Example Usage, column type={l}},
	every head row/.style={before row=\toprule,after row=\midrule},
	every last row/.style={after row=\toprule},
	empty cells with= {--}
]{lexicon.tsv}
\end{document}
